<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.GerenciadorDeGastos"
        tools:targetApi="31">
        <activity
            android:name=".NewGastoActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".SobreActivity"
            android:exported="false" />
        <activity
            android:name=".TiposGastoActivity"
            android:exported="false" />
        <activity
            android:name=".TipoGastoActivity"
            android:exported="false" />
        <activity
            android:name=".MetasActivity"
            android:exported="false" />
        <activity
            android:name=".MetaActivity"
            android:exported="false" />
        <activity
            android:name=".MainListPageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>